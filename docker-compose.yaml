version: '3'
services:
  node-server:
    build: ./server
    ports:
      - "9090:9090"
  envoy:
    build:
      context: ./
      dockerfile: ./envoy/Dockerfile
    image: grpcweb/envoy
    ports:
      - "8080:8080"
  simple-service-client:
    build: ./client
    environment:
      - SECURE_CALL=1
    ports:
      - "8081:8081"